services:
  db:
    image: postgres:14.17
    restart: always
    environment:
      POSTGRES_USER: "pg"
      POSTGRES_DB: "tests"
      POSTGRES_PASSWORD: "password"
    ports:
      - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local
